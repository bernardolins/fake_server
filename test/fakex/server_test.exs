defmodule FailWhale.ServerTest do
  use ExUnit.Case
  doctest FailWhale

  setup do
    :ok
  end

  test "#run return ok and server info when start a new server went well" do
  end
  
  test "#run return error when server already exists" do
  end
end
